import { makeStyles } from "@material-ui/styles";

const useActionCardsPageStyles = makeStyles((theme) => ({
    root: {
        height: '100%',
        width: '100%',
    }
}));

export default useActionCardsPageStyles;