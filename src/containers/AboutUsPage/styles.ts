import { makeStyles } from "@material-ui/styles";

export const useAboutUsPageStyles = makeStyles((theme) => ({
    root: {
        height: '60vh',
        width: '100%',
        position: 'relative',
    },
}));
