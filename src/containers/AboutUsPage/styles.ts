import { makeStyles } from "@material-ui/styles";

export const useAboutUsPageStyles = makeStyles((theme) => ({
    root: {
        height: '100vh',
        width: '100%',
        position: 'relative',
    },
}));
